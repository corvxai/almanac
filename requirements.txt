pydantic>=2.0,<3.0
httpx>=0.27
fastapi>=0.115
uvicorn>=0.34
python-dotenv>=1.0
# Validator-side only: hotkey loading + sr25519 sign/verify. The agent
# runner image MUST NOT install bittensor — keep it in the validator image.
bittensor>=9.0,<11
docker>=7.1
